---
import HomeFeatureCard from "../components/HomeFeatureCard.astro";
import MainLayout from "../layouts/MainLayout.astro";
import {
  capabilityFeatures,
  conversionScopeFeatures,
  extraMediaFeatures,
  videoToolFeatures
} from "../lib/home-content";
import { withBase } from "../lib/paths";
---

<MainLayout
  title="Docufy | Yerel Dosya Dönüştürme"
  description="Docufy ile belge, görsel, ses ve video dönüşümlerini tamamen yerel ortamda yapın."
>
  <section class="hero hero--landing">
    <div class="hero__panel">
      <span class="hero__eyebrow">Go tabanlı CLI + TUI</span>
      <h1>Yerel ortamda production-ready dosya dönüşüm ve medya iş akışları.</h1>
      <p>
        Docufy; belge, görsel, ses ve video işlemlerini tek bir araçta birleştirir. Tek dosyadan operasyonel
        akışlara kadar modern bir dönüştürme altyapısı sunar.
      </p>
      <div class="hero__actions">
        <a class="btn btn--primary" href={withBase("/docs/kurulum/")}>Kuruluma Başla</a>
        <a class="btn btn--secondary" href={withBase("/docs/hizli-baslangic/")}>Hızlı Başlangıç</a>
      </div>
      <ul class="hero__facts">
        <li>Local-first: dosyalar cihazdan çıkmaz</li>
        <li>CLI + TUI: script ve interaktif kullanım aynı binary içinde</li>
        <li>Batch/Watch/Pipeline: operasyonel akışlara hazır</li>
      </ul>
    </div>
  </section>

  <section class="section section--narrow">
    <h2>Docufy ile neler yapabilirsin?</h2>
    <p class="section__lead">
      Özellik seti sadece format dönüşümü değil; medya araçları ve operasyonel akışlar için de güçlü bir
      temel sunar.
    </p>
    <div class="home-grid home-grid--capabilities">
      {capabilityFeatures.map((item) => <HomeFeatureCard item={item} variant="feature" />)}
    </div>
  </section>

  <section class="section section--narrow">
    <h2>Dönüşüm kapsamı</h2>
    <p class="section__lead">Kaynak ve hedef tipleri kategori bazında aşağıdaki kartlarda özetlenmiştir.</p>
    <div class="home-grid home-grid--scope">
      {conversionScopeFeatures.map((item) => <HomeFeatureCard item={item} variant="scope" />)}
    </div>
  </section>

  <section class="section section--narrow">
    <h2>Video araçları</h2>
    <p class="section__lead">Dönüşümün yanında düzenleme ve yardımcı medya araçları da hazır gelir.</p>
    <div class="home-grid home-grid--tools">
      {videoToolFeatures.map((item) => <HomeFeatureCard item={item} variant="compact" />)}
    </div>
  </section>

  <section class="section section--narrow">
    <h2>Ek medya özellikleri</h2>
    <p class="section__lead">Üretim işlerinde sık gereken optimize/normalize/otomasyon ihtiyaçlarını kapsar.</p>
    <div class="home-grid home-grid--extras">
      {extraMediaFeatures.map((item) => <HomeFeatureCard item={item} variant="compact" />)}
    </div>
  </section>

  <section class="section section--narrow">
    <h2>Kısa Demo</h2>
    <div class="demo-frame">
      <img src={withBase("/assets/docufy.gif")} alt="Docufy kısa kullanım demosu" loading="lazy" />
    </div>
  </section>

  <section class="section section--narrow">
    <div class="home-cta-panel">
      <h2>Kullanıma geçmek için dokümantasyonla devam et</h2>
      <p>
        Kurulum, komut örnekleri ve uçtan uca akışlar için docs bölümü adım adım rehber sunar.
      </p>
      <div class="hero__actions">
        <a class="btn btn--primary" href={withBase("/docs/kurulum/")}>Kurulum Rehberi</a>
        <a class="btn btn--secondary" href={withBase("/docs/hizli-baslangic/")}>Kullanım Akışları</a>
      </div>
    </div>
  </section>
</MainLayout>
